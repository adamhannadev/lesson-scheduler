<%= form_with(model: event) do |form| %>
  <% if event.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(event.errors.count, "error") %> prohibited this event from being saved:</h2>

      <ul>
        <% event.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :event_start, style: "display: block" %>
    <%= form.datetime_field :event_start %>
  </div>

  <div>
    <%= form.label :event_end, style: "display: block" %>
    <%= form.datetime_field :event_end %>
  </div>

  <div>
    <%= form.label :description, style: "display: block" %>
    <%= form.text_area :description %>
  </div>

  <div>
          <%= form.label :people, "Teacher", style: "display: block" %>
          <%= collection_select(:person, :person_id, Person.all, :id, :first_name) %>
  </div>
  <div>
          <%= form.label :people, "Teacher", style: "display: block" %>
          <%= collection_select(:person, :person_id, Person.all, :id, :first_name) %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
